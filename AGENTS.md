# Agent Instructions

This repository is a markdown knowledge vault shared by Hermes, Obsidian, and LLM agents.

Rules for every agent live in `CLAUDE.md` — read it first and follow the read order it sets
out. Despite the filename it contains no Claude-specific instruction; it is model-neutral and
applies to GPT, Claude, Codex, or any other LLM that can read and edit this vault.

Procedures live in `projects/second-brain/config/skills/`: `vault-ingest`,
`vault-ingest-claude`, `vault-query`, `vault-lint`, `vault-weekly-report`, `private-note`,
`github-project-link`, `github-project-sync`.
